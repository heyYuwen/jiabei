<!-- 加贝动态 -->
<view class='mustnews'>
 <view class='flex_be newslist' wx:for="{{newslist}}" wx:key='lessonlist' data-item="{{item.id}}" bindtap="todetail">
   <image src="{{item.cover_url}}" mode="widthFix"></image>
   <view class='textright'>
      <text>{{item.title}}</text>
     <view class='itemcontent'>{{item.intro}}</view>
   </view>
 </view>
</view>